import React, { Component } from 'react';

export default class GeneratedParagraph {
  render() {
    return (
      <div>{this.props.text}</div>
    );
  }
}

GeneratedIpsum.propTypes = {
  text: PropTypes.string.isRequired,
};
